# RFC-0002 — 도메인 중립 숙의 실행 엔진

- 상태: Accepted
- 범위: `policy_lab/core`의 실행 엔진과 안전 계약

## 목적

`DeliberationRunner`는 도메인 플러그인이 제공하는 단계 그래프를 순서대로 실행하고, 각 단계의
입출력·승인·출처를 감사 가능한 형태로 기록한다. 특정 행사 순서나 비공개 정책안은 코어에 포함하지
않는다.

## 공개 계약

- 생성 단계의 산출물은 출처 기록이 완전해야 한다.
- 사람 승인이 필요한 산출물은 격리한 뒤 유효한 `ApprovalToken`으로만 공개한다.
- 영향 계산과 생성형 설명은 서로 다른 모델·데이터 경계를 사용한다.
- 단계 실패 시 문맥·투표 원장·출처 기록을 부분 저장하지 않는다.
- 정책안과 질문은 도메인 플러그인의 허용 범위를 벗어날 수 없다.
- 개인 식별정보 대신 실행 범위에서만 유효한 익명 식별자를 사용한다.

## 주요 구성요소

- `runners.py`: 단계 실행과 원자적 커밋
- `stage_handlers.py`: 단계 유형별 처리기
- `approval.py`: 사람 승인 토큰 검증
- `case.py`: 공개 출처와 버전을 가진 케이스 정의
- `ledger.py`: 익명 투표 전이 원장
- `provenance.py`: 모델·프롬프트·근거의 재현 기록

## 검증

방어장치 위반, 승인 토큰 위조·재사용, 출처 누락, 단계 중간 실패, 허용 범위 밖 입력을 자동시험한다.
행사별 실행 순서·운영자 판단·미공개 검토 기록은 공개 저장소의 검증 자료로 사용하지 않는다.
