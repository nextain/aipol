# Contributing to policy-lab

policy-lab은 정책 토론 실험을 *다시 재현할 수 있고, 투명하며, 검증해 반박할 수 있게* 만드는
도구입니다. 기여를 판단하는 첫 번째 기준은 기능 추가가 아니라 **방어장치(신뢰성)를 약화시키지
않는 것**입니다.

코드뿐 아니라 정책 도메인 명세, 근거 검증, 접근성, 문서 번역, 실험 재현, 위협 모델과 실패 사례
기여도 환영합니다. 실제 참가자 데이터나 비공개 행사 자료는 이슈·PR에 첨부하지 마세요.

## 첫 기여 5단계

1. 저장소를 포크하거나 권한이 있는 경우 새 브랜치를 만듭니다.
2. 아래에서 작은 작업 하나를 고릅니다: 공식 근거 링크 검증, 오탈자·번역, 접근성, 테스트, 새 정책
   사례 제안.
3. 변경 이유와 확인 방법을 문서 또는 테스트로 함께 남깁니다.
4. `pytest -q`를 실행하고, 사이트를 바꿨다면 아래 공개 사이트 검사도 실행합니다.
5. Pull Request에 **무엇을 바꿨는지 · 어떻게 확인했는지 · 남은 한계**를 세 문장으로 적습니다.

처음부터 코어 가드나 전체 정책 사례를 바꿀 필요는 없습니다. 코드 없이 공식 근거의 기준일과 원문을
확인하거나, 합성 데이터와 실제 관측의 표현을 점검하는 기여도 동등하게 검토합니다. 저장소가 공개로
전환되기 전에는 write 권한이 있는 공동연구자가 브랜치와 PR로 참여합니다.

## 시작

```bash
pip install -e ".[test]"
pytest -q
```

## 방어장치는 헌장이다 (Charter)

`policy_lab/core/guards.py`의 가드와 `DeliberationProtocol.validate()`의 불변식은 이
프레임워크의 학술적 기여 포인트입니다(RFC §6). 다음과 같은 변경은 **혼자 머지할 수 없으며,
메인테이너 승인과 근거 문서가 반드시 있어야 합니다**.

- A/B 분리(`assert_ab_separation`)를 우회하거나 완화하는 변경
- `human_gate`를 제거하거나 우회 경로를 추가하는 변경
- 변수 화이트리스트(`assert_within_whitelist`)를 무력화하는 변경
- 정밀한 단일 숫자를 허용하는 변경(`reject_single_number` 완화)
- Provenance 기록 항목을 줄이는 변경

방어장치를 바꾸는 PR은 "이 변경이 어떤 적대적 검토(red-team) 약점을 다시 여는가"를 반드시
밝혀야 합니다.

## 새 도메인 추가

1. `policy_lab/domains/<name>/`에 `DomainPlugin`을 구현합니다
   (`levers / fixed_variables / evidence / impact_model / segments / prompts`).
2. `domains/<name>/spec.md` 명세를 작성합니다 (레버, 근거 출처, 영향 모델, 프롬프트, 프로토콜 매핑).
3. `tests/test_<name>.py`를 작성합니다 — 플러그인 형태, 영향 모델 방향, 근거 앵커를 검증합니다.
4. `protocol().validate()`가 통과하는지 확인합니다 (human_gate 포함).

도메인은 코어를 수정하지 않습니다. 코어를 고쳐야 한다면 별도 RFC나 이슈로 분리합니다.

## 근거(Evidence) 규칙

정량 앵커에는 출처와 버전(`source`, `as_of`)을 반드시 적습니다. 검증되지 않은 숫자를 앵커로
넣지 않습니다. 라이브 생성물이 앵커와 어긋나면 `EvidenceStore.contradictions` 규칙이
이를 검출합니다.

## 주장과 데이터 표기

- 합성 페르소나 실행과 실제 참가자 관측을 제목·본문·파일명에서 명확히 구분합니다.
- 계획된 행사에는 미래 시제를 쓰며, 완료 전에는 결과·효과·참가 인원을 확정 사실처럼 쓰지 않습니다.
- 모델 출력은 여론, 대표 표본, 정책 효과 또는 합의의 증거로 표현하지 않습니다.
- 정량 주장은 출처·기준일·실행 조건과 한계를 함께 기록합니다.

## 공개 사이트 변경

`site/`을 수정하면 다음 검사를 실행합니다.

```bash
pytest -q tests/test_public_site.py tests/test_public_site_browser.py
```

브라우저 검사는 Playwright와 Chromium 계열 브라우저가 없으면 건너뜁니다. 새 외부 스크립트,
추적 도구, 폼, 쿠키 또는 검색 노출 변경은 별도 보안·개인정보 검토가 필요합니다. 소프트 론칭 중에는
`noindex`, `X-Robots-Tag`와 전체 차단 `robots.txt`를 제거하지 않습니다.

보안 취약점이나 참가자 정보 노출 가능성은 공개 이슈 대신 [`SECURITY.md`](SECURITY.md)의 비공개
절차로 신고해 주세요. 모든 참여자는 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)를 따릅니다.

## 해외 AI 정책개발 동향

`content/global-ai-policy/` 동향 PR은 정부·국제기구의 공식 HTTPS 원문을 사용합니다. 원문 전체를
번역하거나 재게시하지 않고 한국어 편집 요약, 정책개발 활용 지점, 사람의 검토, 시사점과 한계를
분리합니다. 자동 수집기는 공식 출처 후보만 큐에 넣습니다. Solar Open2가 만든 편집 초안은
Nemotron 3 Ultra의 원문 대조 적대 검토를 통과해야 콘텐츠 레코드가 될 수 있으며, PR 검사와
maintainer 병합을 거친 뒤 사이트·RSS에 반영합니다.

- 제목·날짜·기관과 링크가 원문에 맞는가
- 요약과 숫자가 원문의 의미를 과장하거나 바꾸지 않는가
- AI 일반 정책 뉴스가 아니라 정책개발 방법과 실제로 관련 있는가
- 사람의 판단과 알려진 한계가 빠지지 않았는가
- 원문 문장을 과도하게 복제하지 않았는가

콘텐츠를 수정한 뒤 `python bots/policy_news/build.py`로 HTML과 RSS를 다시 생성합니다.

## 커밋

Conventional Commits 형식(`type(scope): summary`)을 따릅니다. 방어장치와 관련된 변경은
커밋 메시지에 명시합니다.

## License

기여물은 Apache-2.0 라이선스로 제공됩니다.
