# RFC-0001 — policy-lab 프레임워크 아키텍처

- 상태: Draft
- 작성: 2026-06-19
- 범위: 도메인 중립 Human+AI 정책 숙의 실험 프레임워크
- 근거: 공개 정책개발 방법론과 재현 가능한 계약 테스트

---

## 1. 동기

정책 행사의 *라이브 세션 대본*은 특정 상황에 맞춰져 있어 다른 정책에 다시 쓰기 어렵습니다.
또한 일반적인 적대 검토에서 반복되는 약점들 —
프레임 정렬, 집단 의사 선언, "시뮬레이션"이라는 표현의 과장, 닫힌 루프에서의 오류 증폭 —
을 오히려 *연출 의도로 명문화*하고 있습니다.

`policy-lab`의 목표는 이 대본을 **특정 분야에 얽매이지 않는 프로토콜과, 방어장치를 내장한
엔진**으로 끌어올리는 것입니다. 구체적으로는 다음을 지향합니다.
- 같은 골격으로 연금 외의 정책(기후·주거·조세 등)도 실험하고,
- 위의 약점들을 프레임워크 차원에서 강제로 막으며(아래 §6),
- 모든 실행을 다시 재현하고 감사할 수 있도록 기록합니다.

연금 실험은 이 프레임워크를 처음으로 적용한 **레퍼런스 사례**(`domains/pension`)가 됩니다.

## 2. 하지 않는 것 (Non-goals)

- AI가 정책을 *결정*하지 않습니다. 결정 권한은 인간(전문가와 청중)에게 있습니다.
- AI가 여론이나 인간 행동을 *예측*한다고 주장하지 않습니다 (SimBench 점수가 최고 40.8/100로
  낮습니다).
- "정답" 정책을 만들어 내지 않습니다. 산출물은 어디까지나 *탐색, 합성, 기록*입니다.
- 정밀한 단일 숫자를 권위 있는 값으로 제시하지 않습니다 (방향성과 불확실성 범위만 제시합니다).

## 3. 핵심 개념 (도메인 중립)

| 개념 | 정의 | 연금에서의 예 |
|---|---|---|
| **Lever (정책 레버)** | 실험에서 바꿔 가며 보는 변수. 고정 변수와 구분한다 | 수급연령·국고투입·기초연금 구조 (보험료율·소득대체율은 고정) |
| **PolicyOption (정책안)** | 레버 값을 조합한 안. 전문가안 또는 AI 수정안 | 전문가 1안·2안, AI 수정안 |
| **Evidence (근거)** | 출처와 버전을 고정한 팩트체크 자료 묶음 | 보험료율 13%(2033년), 소득대체율 43% 등 교정 숫자 |
| **ImpactModel (영향 모델)** | 레버에서 결과를 *결정론적으로* 계산하는 어댑터(A축) | 재정·세대 효과의 방향을 계산하는 도구 |
| **Scenario (시나리오)** | 정책안을 보여 주는 *예시 서사* (예측 아님) | 각 안의 2045년 미래 모습 |
| **Issue (쟁점)** | 인간이 승인한 찬반 구조 | 수급연령을 늦추기 대 유지하기 등 |
| **VoteRound (투표)** | 세그먼트별 청중의 선택 | 1차·방향·2차 투표 |
| **DeliberationProtocol** | 여러 단계(stage)를 잇는 순서 그래프 | 아래 §5 |
| **ProvenanceRecord** | 모든 입출력·모델·버전·가정을 남기는 감사 로그 | 재현성과 책임 추적 |

## 4. 아키텍처 (포트 / 어댑터)

```
                    ┌─────────────────────────────────────┐
                    │      Session Orchestrator            │
                    │  (라이브 운영·투표 수집·즉시 공개)    │
                    └───────────────┬─────────────────────┘
                                    │ 단계 그래프 실행
              ┌─────────────────────┼─────────────────────┐
              ▼                     ▼                     ▼
     ┌────────────────┐   ┌──────────────────┐   ┌────────────────┐
     │  B: 공론 루프   │   │  Prompt Registry  │   │ A: Impact Engine│
     │ (투표·발화 수집)│   │ (버전·temp·시드   │   │ (결정론 계산,   │
     │                 │   │  고정, 사전검수)  │   │  LLM은 설명만)  │
     └────────────────┘   └──────────────────┘   └────────────────┘
              │                     │                     │
              └─────────────────────┼─────────────────────┘
                                    ▼
                    ┌─────────────────────────────────────┐
                    │  Domain Plugin  (예: pension)         │
                    │  levers · evidence · impact_model ·  │
                    │  segments · prompt set               │
                    └─────────────────────────────────────┘
                                    │ 모든 단계 기록
                                    ▼
                    ┌─────────────────────────────────────┐
                    │  Provenance / Audit Log              │
                    │  (재현성 + 책임 추적)                 │
                    └─────────────────────────────────────┘
```

**핵심 분리 규칙:** A(Impact Engine)와 B(공론 루프)는 *서로 다른 모델, 다른 데이터, 다른
프레이밍*을 써야 합니다. A는 결정론적으로 계산하고, LLM은 그 결과를 *설명*하기만 합니다.
"시뮬레이션"이라는 단어는 A의 결정론적 계산에만 쓰고, B의 LLM 출력에는 쓰지 않습니다.

### 4.1 구성 요소

- **Session Orchestrator** — 단계 그래프를 순서대로 실행합니다. 투표를 모으고, 결과를 즉시
  공개하며, 단계별 시간을 관리합니다.
- **Prompt Registry** — 각 AI 단계의 프롬프트를 버전·temperature·시드와 함께 고정해 보관합니다.
  미리 생성해 검수하는 모드(라이브 행사에서는 연출)를 지원하며, 변수 질문은 화이트리스트에
  올라온 것만 넣을 수 있습니다.
- **Impact Engine (A)** — 레버 조합에서 결과의 방향과 범위(밴드)를 결정론적으로 산출합니다.
  가정과 수식을 모두 공개합니다.
- **공론 루프 (B)** — 세그먼트(연령대 등)별로 투표와 발화를 모읍니다. *참여하지 않은 사람과
  미래 세대가 빠져 있다는 점*을 분명히 라벨로 표시합니다.
- **Domain Plugin** — 레버, 근거 자료 묶음, 영향 모델, 세그먼트, 프롬프트 세트를 한 도메인
  단위로 묶어 캡슐화합니다.
- **Provenance Log** — 모델 ID, 버전, temperature, 시드, 프롬프트, RAG 출처, 계산 가정,
  투표 원자료를 모두 기록합니다.

## 5. 숙의 프로토콜 (단계 그래프)

참조 흐름을 특정 도메인에 얽매이지 않는 stage 유형으로 추상화합니다. 실제 행사의 순서는 비공개
운영 설정으로 주입하며, 아래 표는 소프트웨어 계약을 설명하기 위한 예시입니다.

| # | Stage 타입 | 공개 참조 동작 | 주체 |
|---|---|---|---|
| 1 | `intro` | ① 오프닝·구조 설명 | 사회자 |
| 2 | `expert_input` | ② 전문가안 제시 | 인간 전문가 |
| 3 | `vote(baseline)` | ③ 1차투표 | 청중 |
| 4 | `ai_generate(scenario)` | ④ AI 시나리오(예시 서사) | AI |
| 5a | `ai_generate(issues)` + `human_gate` | ⑤ 쟁점정리(AI) **+ 전문가 승인** | AI→전문가 |
| 5b | `vote(direction)` | ⑤ 방향투표 | 청중 |
| 6 | `expert_comment(risk)` | ⑥ 리스크=비용·제약 | 인간 전문가 |
| 7a | `audience_query(whitelist)` | ⑦ 변수형 질문만 | 청중 |
| 7b | `impact_sim` | ⑦ 변수 변경 영향(A축 계산) | A 엔진 |
| 8 | `expert_comment(criteria)` | ⑧ 가장 중요한 기준 | 인간 전문가 |
| 9 | `ai_generate(revision)` | ⑨ AI 수정안 *합성* | AI |
| 10 | `verify` | ⑩ 수정안 약점 청취 | 청중 |
| 11 | `vote(final)` + `measure(mind_change)` | ⑪ 2차투표·선택변화 | 청중 |
| 12 | `interpret` | ⑫ 결과해석·마무리 | 전체 |

5a 단계의 `human_gate`가 핵심 추가물입니다. 이 프레임워크는 **전문가 승인 없이는 AI가 정리한
쟁점을 청중에게 내보낼 수 없게** 합니다
(프레이밍 권력 방어).

## 6. 방어장치 (적대적 검토 → 프레임워크 가드)

적대적 검토에서 확인한 약점을 프레임워크가 *강제로* 막습니다. 바로 이 부분이
학술적 기여 포인트입니다.

| 적대적 검토가 지적한 약점 | 프레임워크 가드 |
|---|---|
| A/B 닫힌 루프에서 오류 증폭 | A와 B의 모델·데이터·프레이밍을 강제로 분리(같은 LLM 공유 금지) |
| 동의 조작(manufactured consent, "같은 프레임으로 정렬") | 쟁점은 인간 승인 게이트를 거침, "정렬"을 "공통 어휘"로 다시 표현, 대안 프레임을 기록 |
| 표본은 민의가 아님("집단 의사 선언") | "오늘 이 자리의 선택"이라는 라벨을 강제, 참여하지 않은 사람과 미래 세대가 빠졌음을 명시 |
| 생성형 에이전트의 행동 예측이 약함 | 행동 예측 출력 금지, 시나리오에 "예시 서사" 라벨, SimBench 한계 주석 |
| 허위 정밀성(단일 숫자) | 단일 숫자 금지, 방향성과 불확실성 범위(밴드)만 허용 |
| 결과를 재현할 수 없음 | 모델·버전·temperature·시드 고정 + Provenance Log |
| 순환 검증(한 LLM이 생성·요약·평가를 모두 함) | 생성·요약·평가 모델의 역할 분리 |
| 프롬프트 인젝션과 의견군 오염(poisoning) | 변수 질문 화이트리스트, 입력 검증 |
| 굿하트 법칙(참여율·합의율을 핵심 지표로 삼음) | 합의율을 목표로 삼지 않음, 반대와 갈등을 보존해 기록 |
| 수행성과 루카스 비판 | "정책 발표가 현실을 바꾼다"는 점을 한계로 명시, 무리한 외삽 금지 |
| 목적이 뒤섞임(결과가 여론조사처럼 읽힘) | 목적 선언 의무(`assert_declared_purpose`), 여론 측정은 금지 목적 |
| 결과 수치를 전면에 내세움(여론 오용) | 여론 단정 차단(`assert_not_opinion_claim`), '시뮬레이션 조건에서의 응답 패턴' 라벨 강제 |

> 목적 뒤섞임과 수치 전면화는 공개 계약 테스트로 고정해 회귀를 막습니다.

## 7. 검증 전략

- **사전 등록(pre-registration)**: 단계·프롬프트·측정 지표를 실험 전에 고정하고 공개합니다.
- **외부 검증**: "과거 자료에 잘 들어맞는 것은 검증이 아니다"라는 원칙 아래, holdout 정책충격과
  실제 인간 패널을 핵심 개념으로 둡니다.
- **측정 지표**: 세그먼트별 선택 변화(mind-change), 합의·반대 분포(둘 다 보존), 프레이밍 민감도.
- **재현 패키지**: 세션마다 Provenance Log, 프롬프트, Evidence 버전을 묶어 산출합니다.

## 8. 도메인 교체 (plugin)

도메인 플러그인이 제공해야 할 인터페이스는 다음과 같습니다.

```
DomainPlugin:
  levers: List[Lever]                 # 변화 변수 (+ 고정 변수)
  evidence: EvidenceStore             # 출처·버전 고정 코퍼스
  impact_model: ImpactModel           # 레버 → 결과(결정론)
  segments: List[Segment]             # 청중 세그먼트 정의
  prompts: PromptSet                  # 단계별 프롬프트(버전 고정)
```

프로토콜(§5), 방어장치(§6), 오케스트레이터는 도메인과 상관없이 공통으로 씁니다.

## 9. 남은 과제

- `impact_model`을 어디까지 직접 구현하고, 어디서부터 기존 추계 모형(NABO·복지부·KDI·OECD)을
  어댑터로 끌어다 쓸지.
- 라이브 투표 수집 채널(현장 단말, 모바일) — 인젝션과 중복 투표를 어떻게 막을지.
- 다국어 작업(발표·논문 영문화)을 언제 진행할지.
- 라이선스와 거버넌스(정책학회 공동 자산으로 삼을 범위).

## 10. 다음 단계

1. `domains/pension/spec.md` — 연금 레퍼런스 명세 (이 RFC와 함께 작성).
2. (승인 후) 코드 골격 만들기: `core/{levers,evidence,impact,deliberation,provenance}`와 `domains/pension`.
3. 8/12 세션용 라이브 하네스를 만들어, 이 프레임워크를 처음으로 실제 사용해 보기.
