"use strict";
const chatQuery=document.getElementById("chat-query"),chatError=document.getElementById("chat-error"),chatAnswer=document.getElementById("chat-answer");
const escapeHtml=(value)=>String(value??"").replace(/[&<>"']/g,(c)=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
document.getElementById("chat-send").onclick=async()=>{chatError.textContent="";chatAnswer.classList.add("hidden");try{const response=await fetch("/api/aipol/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:chatQuery.value})});const data=await response.json();if(!response.ok)throw new Error(data.detail||"질문을 처리하지 못했습니다");document.getElementById("chat-text").textContent=data.answer;document.getElementById("chat-sources").innerHTML=data.citations.map((source)=>`<li><a href="${escapeHtml(source.source_url)}" target="_blank" rel="noopener">${escapeHtml(source.title)}</a> · ${escapeHtml(source.content_hash.slice(0,12))}</li>`).join("");document.getElementById("chat-mode").textContent=`${data.notice} · 응답 모드: ${data.mode}`;chatAnswer.classList.remove("hidden");}catch(error){chatError.textContent=error.message;}};
